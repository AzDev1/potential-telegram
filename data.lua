--JSON point values
if value == main then
  add space to user
  if date = 2 then
    -- App requests
    add value pull req
    add value under wlan 
    req "t-cash" from server(val()
     value spec = 1 )
    script@tag:rgb(00,00,00)
    --Linking
    email("nesakazi2016@gmail.com") == 192.187.0.1
    link email with value
    if internet is on then
      --Loading
      load(data) out of (value)
      if successful then send()
        server:unpack()
        load server
        --Uploading
        when("township") launches upload value to server
        add "coins" = 1000000
        upload data to server:unpack(township, swiftisland)
        add value to data::package
        finish while data is synced(+)
        upload:json.lua
        --Bluetooth pairing
        pair() == remote
        link remote bt
        --Buttons
        link 1 == server.msi
        
